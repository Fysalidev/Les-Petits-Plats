const catalogue = new Catalogue(recipes)

const filterBtn = document.querySelectorAll(".search_btn");

/* openList = () => {
  arrowBtn.classList.replace("fa-chevron-down", "fa-chevron-up")
  list.classList.replace("list-hidden","list-show")
}

closeList = () => {
  arrowBtn.classList.replace("fa-chevron-up", "fa-chevron-down");
  list.classList.replace("list-show", "list-hidden");
}

toggle = () => (btn.className === "fa-solid fa-chevron-down") ? openList() : closeList()


filterBtn.forEach(btn => {
  const input = btn.firstElementChild
  const arrowBtn = btn.lastElementChild.firstElementChild
  const list = btn.parentElement.lastElementChild
  
  console.log(input)
  console.log(arrowBtn)
  console.log(list)

  arrowBtn.addEventListener('click', toggle())
  input.addEventListener('focus', toggle())
}) */





